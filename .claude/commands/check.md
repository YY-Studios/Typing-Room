Run quality checks for this project in the following order:

1. Run `pnpm lint` and capture the full output
2. Run `pnpm type-check` and capture the full output
3. Report results:
   - If all checks pass: print a clear success message
   - If any check fails: list each error with file path and line number
   - Do NOT auto-fix anything — just report

Keep the report concise and scannable.
